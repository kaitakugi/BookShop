import 'package:flutter/material.dart';

class Vip3Page extends StatelessWidget {
  const Vip3Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("VIP 1")),
      body: const Center(child: Text("Chào mừng đến với gói VIP 1")),
    );
  }
}
